<% if place.photo.url.present?  %>
  <%= link_to image_tag(place.photo.url , alt: place.name, size: '80x60'),  place_path(place) %>
  <%# image_tag(place.photo.url , alt: place.name, size: '80x60') %>
  <%= place.name %>
<% else %>
  <%= image_tag("no-image.png" , alt: place.name) %>
<% end %>
