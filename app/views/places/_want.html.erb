<%= link_to "" , ownerships_path(place_id: place.id , comment: nil, photo: nil,  type: "Want") ,
    :class => "want-#{place.id} btn-want btn btn-danger btn-sm want",
    :remote => true,
    :method => :post %>
