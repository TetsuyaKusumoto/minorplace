<% if params[:type] == "Visit" %>
  $(".visit-<%= @place.id %>").replaceWith("<%= escape_javascript(render('places/visit' , place: @place)) %>");
<% else %>
  $(".want-<%= @place.id %>").replaceWith("<%= escape_javascript(render('places/want' , place: @place)) %>");
<% end %>

$('.btn-visit,.btn-want').removeAttr('disabled');
